---
// src/layouts/HubLayout.astro
// Used for mini-hub pages: /scavenger-hunts/pirate/, /treasure-hunts/pirate/ etc.
// Different from SiteLayout — no product schema, CollectionPage schema instead

import '../styles/global.css'
import '../styles/theme-scavenger.css'

const {
  seo_title,
  meta_description,
  canonical_url,
  og_image,
  is_staging = true,
  structured_data = [],
  silo = 'scavenger',
} = Astro.props
---

<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{seo_title}</title>
  <meta name="description" content={meta_description} />
  <link rel="canonical" href={canonical_url} />
  <meta name="robots" content={is_staging ? 'noindex,nofollow' : 'index,follow,max-image-preview:large'} />
  <meta name="theme-color" content="#5C3D22" />
  <meta name="author" content="ThinkMystery" />
  <meta property="og:site_name" content="ThinkMystery" />
  <meta property="og:type" content="website" />
  <meta property="og:title" content={seo_title} />
  <meta property="og:description" content={meta_description} />
  <meta property="og:url" content={canonical_url} />
  <meta property="og:image" content={og_image} />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={seo_title} />
  <meta name="twitter:description" content={meta_description} />
  <meta name="twitter:image" content={og_image} />
  {structured_data.map(item => (
    <script type="application/ld+json" set:html={JSON.stringify(item)} />
  ))}
</head>
<body class={`theme-${silo}`}>
  <header class="site-header">
    <a href="/" class="logo" aria-label="ThinkMystery home">ThinkMystery</a>
    <nav aria-label="Main navigation">
      <a href="/scavenger-hunts/">Scavenger Hunts</a>
      <a href="/treasure-hunts/">Treasure Hunts</a>
      <a href="/escape-rooms/">Escape Rooms</a>
    </nav>
  </header>
  <slot />
  <footer>
    <p>© {new Date().getFullYear()} <strong>ThinkMystery</strong> · Printable mystery adventures for kids.<br />
    <small>The only mystery games tested with real kids before we sell them.</small></p>
  </footer>
</body>
</html>
